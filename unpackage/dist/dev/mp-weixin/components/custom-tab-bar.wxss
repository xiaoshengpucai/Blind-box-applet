/**
 * 这里是uni-app内置的常用样式变量
 *
 * uni-app 官方扩展插件及插件市场（https://ext.dcloud.net.cn）上很多三方插件均使用了这些样式变量
 * 如果你是插件开发者，建议你使用scss预处理，并在插件代码中直接使用这些变量（无需 import 这个文件），方便用户通过搭积木的方式开发整体风格一致的App
 *
 */
/**
 * 如果你是App开发者（插件使用者），你可以通过修改这些变量来定制自己的插件主题，实现自定义主题功能
 *
 * 如果你的项目同样使用了scss预处理，你也可以直接在你的 scss 代码中使用如下变量，同时无需 import 这个文件
 */
/* 颜色变量 */
/* 行为相关颜色 */
/* 文字基本颜色 */
/* 背景颜色 */
/* 边框颜色 */
/* 尺寸变量 */
/* 文字尺寸 */
/* 图片尺寸 */
/* Border Radius */
/* 水平间距 */
/* 垂直间距 */
/* 透明度 */
/* 文章场景相关 */
.container.data-v-8fac706f {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 160rpx;
  display: flex;
  align-items: center;
  background: #FFFFFF;
  backdrop-filter: blur(10px);
  box-shadow: 0px -1px 10px rgba(0, 0, 0, 0.1);
  -webkit-backdrop-filter: blur(10px);
  z-index: 999;
}
.tabbar-item.data-v-8fac706f {
  flex: 1;
  display: flex;
  justify-content: center;
  align-items: center;
  position: relative;
}
.icon-wrapper.data-v-8fac706f {
  display: flex;
  justify-content: center;
  align-items: center;
  transform: translateY(-25rpx);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}
.icon.data-v-8fac706f {
  width: 80rpx;
  height: 80rpx;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  transform-origin: center bottom;
  will-change: transform;
  filter: drop-shadow(0 5rpx 5rpx rgba(255, 0, 0, 0.3));
}
.text-label.data-v-8fac706f {
  display: flex;
  justify-content: center;
  align-items: center;
}
.label.data-v-8fac706f {
  font-size: 28rpx;
  font-weight: bold;
  color: #999;
  position: absolute;
  opacity: 1;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  will-change: transform, opacity;
}
.active .icon-wrapper.data-v-8fac706f {
  transform: translateY(-50rpx);
}
.active .label.data-v-8fac706f {
  color: red;
}
.highline.data-v-8fac706f {
  width: 100rpx;
  height: 100rpx;
  padding: 12rpx;
  position: fixed;
  transform: translate(-50%, -50%);
  /* 透明边框 */
  border-radius: 50%;
  /* 假设圆形凸起，可根据需求调整 */
  box-shadow: 0 2rpx 2rpx rgba(0, 0, 0, 0.1);
  /* 模拟凸起阴影 */
  pointer-events: none;
  /* 不影响点击 */
  transition: all 0.3s ease;
  /* 过渡动画 */
  z-index: -1;
  -webkit-mask: radial-gradient(circle at center, transparent 60%, rgba(0, 0, 0, 0.5) 51%);
  mask: radial-gradient(circle at center, transparent 60%, rgba(0, 0, 0, 0.5) 51%);
  background: rgba(0, 0, 0, 0.5);
}